# Post_Management_System
A RESTful post management application built with Node.js, Express, and EJS.

## Features
- View all posts
- Create, edit, and delete posts
- Dynamic frontend with EJS templating

## Setup
1. Clone the repo
2. Run `npm install`
3. Run `node index.js`
4. Open `http://localhost:8080/posts`
